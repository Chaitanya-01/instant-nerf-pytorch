Make sure when you download the lego dataset, it follows this file strucutre:
```bash
data/
└── nerf_synthetic
    └── lego
        ├── test
        ├── train
        ├── transforms_test.json
        ├── transforms_train.json
        ├── transforms_val.json
        └── val
```